<%- include(`partials/header.ejs`) %> 
<%- include(`partials/navbar.ejs`) %> 
    <h1>Random Number: <%= random %></h1>
    <% if (random % 2 === 0) { %>
    <h3>Genap</h3>
    <% } else { %>
    <h3>Ganjil</h3>
    <% } %>

    <h2><%= random%2 === 0 ? `even` : `odd` %></h2>

    <%- include(`partials/footer.ejs`) %>     
